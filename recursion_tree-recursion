// this is for the tree recursion


#include <iostream>
using namespace std;

int fun(int n)
{
    if (n > 0)
    {
        fun(n - 1);
        cout << n<<" ";
        fun(n - 1);
    }
    return 0;
}

int main()
{
    int x = 3;
    cout << fun(x);
    return 0;
}
